import React from 'react'

export const EditView = ({ id }) => (
    <div>spacer {id}</div>
)

export const RenderView = ({ id }) => (
    <div>spacer {id}</div>
)

export default {
    EditView,
    RenderView
}