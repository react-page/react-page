var React = require('react');

module.exports = React.createClass({
    render: function () {
        return (
            /*jshint ignore:start */
            <div style={{backgroundColor: 'red'}}>
                I could not find that component. :(
            </div>
            /*jshint ignore:end */
        );
    }
});
